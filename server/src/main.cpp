// Server main entry point (placeholder)
#include <iostream>

int main(int argc, char* argv[]) {
    std::cout << "R-Type Server starting..." << std::endl;
    // TODO: Initialize server, bind UDP socket, run game loop
    return 0;
}
